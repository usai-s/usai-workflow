---
description: Этапы 4–5 — backend-разработка и backend-QA
argument-hint: <id фичи>
---

Фича: $ARGUMENTS

1. Проверь гейты в status.md (для feature-задач нужен `gate-2: approved`
   или отметка, что дизайн не требуется).
2. Запусти субагента **backend-developer** (этап 4: план → контракт → код).
3. Затем субагента **qa-engineer** с указанием «backend» (этап 5) — отдельным
   вызовом, чистый контекст.
4. Дефекты-блокеры → верни backend-developer'у; максимум 2 цикла, дальше
   эскалация человеку. По завершении сообщи итог и путь к qa.md (раздел Backend).
