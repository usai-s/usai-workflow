---
description: Этап 3 — UX/UI дизайн + подготовка gate-2
argument-hint: <id фичи>
---

Фича: $ARGUMENTS

1. Проверь в `docs/features/$ARGUMENTS/status.md`, что `gate-1: approved`.
   Если нет — остановись и сообщи.
2. Запусти субагента **ux-designer**.
3. Покажи человеку design.md (и прототип, если есть), попроси вердикт по
   gate-2 и запиши его в status.md.
