# Конвенции backend — ЗАГОТОВКА

<!-- Seed-файл: заполните под стек проекта. Пайплайн ссылается сюда как на
     источник правил кода: backend-developer читает этот файл перед планом и
     кодом (этап 4), code-reviewer сверяет с ним архитектуру (этап 8). -->

Минимум, который должен быть зафиксирован:

## Архитектура

- Слои/границы: <какие слои существуют, направление зависимостей>.
- Структура фичи/слайса: <куда кладётся новый endpoint/handler/домен>.
- Что запрещено: <например, слайсы не ссылаются друг на друга; домен чист от
  инфраструктурных пакетов>.

## Данные и миграции

- <СУБД, ORM/механизм миграций, как миграции применяются (явно/на старте),
  правила именования>.

## Ошибки и контракты

- <Модель ошибок: исключения/Result, формат ответов об ошибке, где живут
  коды и тексты>.

## Тесты

- <Виды тестов и инструменты: unit на домен, integration на endpoint против
  реальной БД; что считается достаточным покрытием критерия спеки>.

## Стиль

- <Линтеры/форматтеры, warnings-as-errors, нейминг>.
