# Конвенции frontend — ЗАГОТОВКА

<!-- Seed-файл: заполните под стек проекта. Пайплайн ссылается сюда как на
     источник правил кода: ux-designer называет компоненты по именам из
     дизайн-системы (этап 3), frontend-developer читает файл перед кодом
     (этап 6). -->

Минимум, который должен быть зафиксирован:

## Стек и дизайн-система

- <Фреймворк, библиотека компонентов/дизайн-система, тема>.
- Канон состояний экрана: loading / empty / error / success / права —
  <как каждое выражается компонентами>.

## Структура

- <Раскладка кода: фичи-слайсы, страницы/роуты, общие компоненты; что
  запрещено (слайсы не лезут во внутренности друг друга)>.

## Состояние и API

- <Server state / client state: инструменты; API-слой строго по
  api-contract.md; инструмент моков до готовности backend>.

## Тесты

- <Unit, компонентные, e2e: инструменты и что считается достаточным>.

## Стиль

- <Линтер, форматтер, правила текстов интерфейса (язык, откуда берутся)>.
